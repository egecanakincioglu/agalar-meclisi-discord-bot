services:
  agalar-meclisi-bot:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: agalar-meclisi-bot

    restart: unless-stopped

    volumes:
      - ./config:/app/config
      - ./data:/app/data

    environment:
      NODE_ENV: production
      TZ: Europe/Skopje
